- name: Install Arch packages
  community.general.pacman:
    name: kanshi
    state: present
  when: ansible_facts['distribution'] == 'Archlinux'

- name: Apply Kanshi config
  ansible.builtin.shell: 'stow --target=${HOME} kanshi'
  args:
    chdir: '{{ playbook_dir }}/../dotfiles'
  become: true
  become_user: '{{ user }}'
